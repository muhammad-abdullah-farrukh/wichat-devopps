services:
  mongodb:
    container_name: mongodb-wichat_en3b
    image: mongo
    profiles: ["dev", "prod"]
    volumes:
      - mongodb_data:/data/db
    ports:
      - "27017:27017"
    networks:
      - mynetwork

  authservice:
    container_name: authservice-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-authservice:latest
    profiles: ["dev", "prod"]
    build: ./users/authservice
    depends_on:
      - mongodb
    ports:
      - "8002:8002"
    networks:
      - mynetwork
    environment:
      MONGODB_URI: mongodb://mongodb:27017/userdb

  userservice:
    container_name: userservice-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-userservice:latest
    profiles: ["dev", "prod"]
    build: ./users/userservice
    depends_on:
      - mongodb
    ports:
      - "8001:8001"
    networks:
      - mynetwork
    environment:
      MONGODB_URI: mongodb://mongodb:27017/userdb

  questionservice:
    container_name: questionservice-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-questionservice:latest
    profiles: ["dev", "prod"]
    build: ./questionservice
    depends_on:
      - mongodb
    ports:
      - "8004:8004"
    networks:
      - mynetwork
    environment:
      MONGODB_URI: mongodb://mongodb:27017/mongo-db-wichat_en3b

  llmservice:
    container_name: llmservice-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-llmservice:latest
    profiles: ["dev", "prod"]
    build:
      context: ./llmservice
      args:
        LLM_API_KEY: ${LLM_API_KEY}
        GEMINI_LLM_API_KEY: ${GEMINI_LLM_API_KEY}
    environment:
      LLM_API_KEY: ${LLM_API_KEY}
      GEMINI_LLM_API_KEY: ${GEMINI_LLM_API_KEY}
    ports:
      - "8003:8003"
    networks:
      - mynetwork

  gatewayservice:
    container_name: gatewayservice-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-gatewayservice:latest
    profiles: ["dev", "prod"]
    build: ./gatewayservice
    depends_on:
      - mongodb
      - userservice
      - authservice
      - llmservice
      - questionservice
    ports:
      - "8000:8000"
    networks:
      - mynetwork
    environment:
      FRONTEND_URL: ${FRONTEND_URL}
      AUTH_SERVICE_URL: http://authservice:8002
      USER_SERVICE_URL: http://userservice:8001
      LLM_SERVICE_URL: http://llmservice:8003
      QUESTION_SERVICE_URL: http://questionservice:8004
      GRAFANA_URL: http://grafana:9091

  webapp:
    container_name: webapp-wichat_en3b
    image: ${ECR_REGISTRY}/wichat-webapp:latest
    profiles: ["dev", "prod"]
    build:
      context: ./webapp
      args:
        REACT_APP_API_ENDPOINT: ${REACT_APP_API_ENDPOINT}
    depends_on:
      - gatewayservice
    ports:
      - "3000:3000"

  prometheus:
    image: prom/prometheus
    container_name: prometheus-wichat_en3b
    profiles: ["dev", "prod"]
    networks:
      - mynetwork
    volumes:
      - ./gatewayservice/monitoring/prometheus:/etc/prometheus
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    depends_on:
      - gatewayservice

  grafana:
    image: grafana/grafana
    container_name: grafana-wichat_en3b
    profiles: ["dev", "prod"]
    networks:
      - mynetwork
    volumes:
      - grafana_data:/var/lib/grafana
      - ./gatewayservice/monitoring/grafana/provisioning:/etc/grafana/provisioning
    environment:
      - GF_SERVER_HTTP_PORT=9091
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_AUTH_PROXY_ENABLED=true
      - GF_AUTH_PROXY_HEADER_NAME=X-WEBAUTH-USER
      - GF_AUTH_PROXY_HEADER_PROPERTY=username
      - GF_AUTH_PROXY_AUTO_SIGN_UP=true
      - GF_SERVER_ROOT_URL=http://localhost:8000/admin/monitoring/
      - GF_SERVER_SERVE_FROM_SUB_PATH=true
    expose:
      - "9091"
    depends_on:
      - prometheus

volumes:
  mongodb_data:
  prometheus_data:
  grafana_data:

networks:
  mynetwork:
    driver: bridge
